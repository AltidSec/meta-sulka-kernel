# SPDX-License-Identifier: MIT
define KFEATURE_DESCRIPTION "Harden userspace"
define KFEATURE_COMPATIBILITY all

kconf non-hardware sulka-harden-userspace.cfg

if [ "$KARCH" = "x86_64" ]; then
    kconf non-hardware sulka-harden-userspace-x86_64.cfg
fi
