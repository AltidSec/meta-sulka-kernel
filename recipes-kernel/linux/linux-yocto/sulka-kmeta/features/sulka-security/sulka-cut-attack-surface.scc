# SPDX-License-Identifier: MIT
define KFEATURE_DESCRIPTION "Disable features to reduce kernel attack surface"
define KFEATURE_COMPATIBILITY all

kconf non-hardware sulka-cut-attack-surface.cfg

if [ "$KARCH" = "x86_64" ]; then
    kconf non-hardware sulka-cut-attack-surface-x86_64.cfg
fi

